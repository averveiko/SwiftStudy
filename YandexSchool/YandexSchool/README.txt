Задание 1 (лекция https://youtu.be/ik3Jw-GCzUY?list=PLQC2_0cDcSKBUXhSGqAbVAp3SFBKPnpFI)

Реализовать структуру TodoItem (model)
    immutable struct (all let)
    Содержит уникальный id, если не задан пользователем -генерируется (UUID().uuidString)
    Содержит обязательное cтроковое поле - text
    Содержит обязательное  важность (enum: неважная, обычная, важная)
    Содержит дедлайн, может быть не задан, если задан - дата.
    
Реализовать расширение TodoItem для разбора json
    Содержит функцию (static func parse(json: Any) -> TodoItem?) для разбора json
    Содержит вычислимое свойство (var json: Any) для формирования json’а
    Не сохранять в json важность, если она обычная
    Не сохранять в json сложные объекты ( Date) //use unixtimestamp
    Сохранять deadline только если он задан
    Обязательно использовать JSONSerialization (т.е. Работу со словарем)

Реализовать класс FileCache
    Содержит закрытую для внешнего изменения, но открытую для получения коррекцию TodoItem
    Содержит функцию добавления новой задачи
    Содержит функцию удаления задачи (по id)
    Содержит функцию сохранения всех дел в файл
    Содержит функцию загрузки всех дел из файла
    Можем иметь несколько разных файлов

Реализовать сохранение и загрузку FileCache в файл и из файла
